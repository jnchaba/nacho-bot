const { Channel } = require('discord.js');

module.exports = {
    name:'test',
    description:'delete old stuff',
    execute(message, args) {
        //const channel = message.guild.channels.find("id","448400100591403024");
        //message.member.setChannel(channel);
    }
}